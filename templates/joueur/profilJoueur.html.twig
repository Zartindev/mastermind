{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1 class="mb-3">Voici votre profil</h1>

    <table class="table">
        <thead>
            
        </thead>
        <tbody>
        {% for joueur in joueurs %}
            <tbody>
            {#<tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>#}
            <tr>
                <th>Username</th>
                <td>{{ joueur.username }}</td>
            </tr>
            {#<tr>
                <th>Roles</th>
                <td>{{ joueur.roles ? joueur.roles|json_encode : '' }}</td>
                "{{ path('app_joueur_edit', {'id': joueur.id})}}"
            </tr>#}
            <tr>
                <th>Email</th>
                <td>{{ joueur.email }}</td>
            </tr>
            <tr>
                <th>ImgProfil</th>
                <th><img src="/img/{{ joueur.imgProfil}}" width= 200px></th>
            </tr>
            <tr>
                <th>ImgBanniere</th>
                <th><img src="/img/{{joueur.imgBanniere}}" width= 200px></th>
            </tr>
            <tr>
               <th><a class="btn" href="{{ path('app_joueur_edit', {'id': joueur.id})}}" >Modifier son profil</a></th>
            </tr>
        </tbody>
        {% else %}
            <tr>
                <td colspan="8">Vous n'etes pas connecté</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
